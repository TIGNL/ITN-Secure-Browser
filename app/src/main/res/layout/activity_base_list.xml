<?xml version="1.0" encoding="utf-8"?>
<!--
  activity_base_list.xml
  الهيكل المشترك لجميع صفحات القوائم الكاملة:
    • Header 64dp مع زر رجوع على اليسار وعنوان في المنتصف
    • Divider
    • RecyclerView للمحتوى
  الاستخدام: استدعِ <include layout="@layout/activity_base_list"/> أو ورّث من BaseListActivity
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg_toolbar">

    <!-- Header 64dp -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="@color/bg_toolbar">

        <!-- زر الرجوع -->
        <ImageButton
            android:id="@+id/btnBack"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:src="@drawable/ic_back"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Back" />

        <!-- العنوان في المنتصف -->
        <TextView
            android:id="@+id/listHeaderTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center"
            android:textColor="@color/text_primary"
            android:textSize="16sp" />

        <!-- Spacer لموازنة زر الرجوع -->
        <View
            android:layout_width="64dp"
            android:layout_height="64dp" />

    </LinearLayout>

    <!-- Divider -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#2A2A4A" />

    <!-- المحتوى -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/baseRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

</LinearLayout>
