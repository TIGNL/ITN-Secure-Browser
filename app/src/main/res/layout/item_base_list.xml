<?xml version="1.0" encoding="utf-8"?>
<!--
  item_base_list.xml
  القالب الموحد لعناصر قوائم BaseListActivity.

  الاتفاق: كل صفحة ترث من BaseListActivity تستخدم هذا القالب حصراً.
  المكون المستخدم هو com.google.android.material.card.MaterialCardView
  من مكتبة Material You 1.12.0 لضمان توحيد التصميم تلقائياً.
-->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="0dp"
    android:layout_marginVertical="0dp"
    app:cardElevation="0dp"
    app:strokeWidth="0dp"
    app:rippleColor="?attr/colorControlHighlight">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:minHeight="64dp">

        <!-- الأيقونة -->
        <ImageView
            android:id="@+id/itemIcon"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginStart="8dp"
            android:padding="8dp"
            android:visibility="gone" />

        <!-- النص -->
        <TextView
            android:id="@+id/itemTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:paddingStart="16dp"
            android:textColor="?attr/colorOnSurface"
            android:textAppearance="?attr/textAppearanceBodyLarge" />

        <!-- عنصر اليمين -->
        <FrameLayout
            android:id="@+id/itemEnd"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="8dp"
            android:visibility="gone" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
